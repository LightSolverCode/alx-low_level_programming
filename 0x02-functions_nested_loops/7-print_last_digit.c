#include "main.h"
#include <stdio.h>
/**
 * print_last_digit - check the code
 * @a: the argument
 * Return: Always 0.
 */
int print_last_digit(int a)
{
	int b = a % 10;
	int c = a % 10;
	
	_putchar(b);
	_putchar(a);

	return (0);
}
