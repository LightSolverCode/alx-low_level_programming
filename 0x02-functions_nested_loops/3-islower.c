#include "main.h"
#include <ctype.h>
#include <stdio.h>
/**
 * _islower(int c) -> returns lower case
 * return: returns 1 or 0
 */
int _islower(int c)
{
	if (islower('c'))
	{
		putchar('1');
	}
	else
	{
		putchar('0');
	}
}
